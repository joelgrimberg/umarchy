# Make Omarchy's bin/ available to systemd-user-launched processes.
# Hyprland-spawned processes get this via the env= line in hypr/autostart.conf;
# this file covers anything started by the systemd user manager.
PATH=$HOME/.local/share/omarchy/bin:$PATH
